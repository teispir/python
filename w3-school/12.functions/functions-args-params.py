def my_function(fname, lname): # A "parameter" is the variable listed inside the parentheses in the function definition.
    print(fname + " " + lname)

my_function("Emil", "Refsnes") # An "sargument" is the value that is sent to the function when it is called.