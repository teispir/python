# creation of a tuple with a single element: remember comma!
thistuple = ("apple",)
print(type(thistuple))

# NOT a tuple (because is without comma): it is recognized as string
thistuple = ("apple")
print(type(thistuple))